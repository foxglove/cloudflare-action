{
  "name": "@foxglove/cloudflare-action",
  "private": true,
  "license": "MIT",
  "description": "Deploy to Cloudflare Pages & Workers",
  "type": "module",
  "main": "dist/index.cjs",
  "packageManager": "yarn@4.13.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.cjs",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "test": "node --test src/*.test.ts",
    "typecheck": "tsc --noEmit",
    "ci": "yarn fmt && yarn typecheck && yarn build && yarn test"
  },
  "dependencies": {
    "@actions/core": "3.0.0",
    "@actions/exec": "3.0.0",
    "@octokit/rest": "22.0.1"
  },
  "devDependencies": {
    "@foxglove/tsconfig": "3.2.0",
    "@types/node": "25.5.0",
    "esbuild": "0.27.4",
    "prettier": "^3.8.1",
    "typescript": "5.9.3"
  }
}
